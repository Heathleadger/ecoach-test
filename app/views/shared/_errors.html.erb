<% if obj.errors.any? %>
<div class="alert alert-warning" role="alert">
    
    <h2> Error that prevented the article of being created </h2>
    <ul>
      <% obj.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
      <% end %>
    </ul>
</div>
<% end %>